# 🎯 كيف تستخدم السيرفر الآن

## ✅ الحل الجديد البسيط:

السيرفر الآن يقرأ من ملف `public/features.json`

---

## 📝 لتعطيل أي قسم:

### الطريقة 1: من لوحة التحكم (الأسهل)

1. **افتح:** https://cccxxxzzssee.netlify.app/
2. **عطّل** أي قسم تريده
3. **اضغط "حفظ التغييرات"**
4. **سينزّل** ملف `features.json` 
5. **أرسله لي** وسأرفعه على GitHub فوراً!

### الطريقة 2: يدوياً من GitHub

1. افتح: https://github.com/zrtbytbt-ux/nnn999
2. اذهب لمجلد `public`
3. افتح ملف `features.json`
4. اضغط أيقونة القلم (Edit)
5. غيّر `true` إلى `false` للقسم الذي تريد تعطيله
6. **مثال** - لتعطيل الغرف الصوتية:
```json
{
  "voiceRooms": false,
  "tribes": true,
  "adhkar": true,
  "quizzes": true
}
```
7. اضغط **"Commit changes"**
8. انتظر دقيقة - Netlify سيحدّث تلقائياً
9. ✅ التطبيق سيخفي القسم!

---

## 🧪 اختبار:

```bash
curl https://cccxxxzzssee.netlify.app/api/get-features
```

**يجب أن يُظهر** التغييرات من `features.json`!

---

## 📱 للتطبيق:

نفس الكود - لا تغيير:

```dart
final features = await RemoteFeatureControl.getFeatures();

if (features['voiceRooms'] == false) {
  // الغرف الصوتية معطّلة - لا تعرضها
}
```

---

## ✨ المميزات:

- ✅ **بسيط جداً** - ملف JSON واحد فقط
- ✅ **لا Token مطلوب**
- ✅ **لا Environment Variables**
- ✅ **التحديث فوري** عبر GitHub
- ✅ **يعمل مع التطبيق**

---

## 🎯 الخلاصة:

**الآن:** عدّل `public/features.json` على GitHub وسيتحدث التطبيق تلقائياً! 🚀

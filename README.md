# سيرفر التحكم عن بعد - Remote Config Server 🎮

## ما هذا؟
سيرفر بسيط يتيح لك التحكم في التطبيق عن بعد بدون تحديث APK!

## الميزات:
- ✅ تفعيل/إيقاف وضع الصيانة
- ✅ إجبار التحديث
- ✅ إضافة/إزالة أقسام ديناميكياً
- ✅ التحكم في أزرار الوصول السريع
- ✅ تغيير نصيحة اليوم
- ✅ إضافة إعلانات

---

## 🚀 نشر على Netlify (مجاني)

### الخطوة 1: إنشاء Repository على GitHub

```bash
cd c:\Users\user\Music\jos\test7\remote-config
git init
git add .
git commit -m "Remote config server"
```

ثم أنشئ repo جديد على github.com وارفعه.

### الخطوة 2: ربط مع Netlify

1. اذهب إلى [netlify.com](https://netlify.com)
2. سجل دخول بحساب GitHub
3. اضغط "Add new site" → "Import an existing project"
4. اختر الـ repository الذي أنشأته
5. اضغط "Deploy site"
6. ✅ ستحصل على رابط مثل: `https://xyz-123.netlify.app`

### الخطوة 3: تحديث رابط التطبيق

افتح الملف `lib/services/remote_config_service.dart` وغيّر:

```dart
static const String _configUrl = 'https://YOUR-SITE.netlify.app/config.json';
```

إلى رابطك الجديد.

---

## 📝 كيفية الاستخدام

### تفعيل وضع الصيانة:
```json
"maintenance": {
  "enabled": true,
  "message": "التطبيق تحت الصيانة حتى الساعة 10 مساءً"
}
```

### إضافة زر جديد في الوصول السريع:
```json
"quickAccess": [
  {
    "id": "new_feature",
    "name": "ميزة جديدة",
    "icon": "star",
    "color": "#FFD700",
    "route": "/new",
    "enabled": true,
    "order": 3
  }
]
```

### إضافة قسم ديناميكي:
```json
"dynamicSections": [
  {
    "id": "promo",
    "title": "🎁 عرض خاص!",
    "type": "announcement",
    "enabled": true,
    "order": 1,
    "content": {
      "message": "خصم 50% لفترة محدودة!",
      "color": "#FF6B6B"
    }
  }
]
```

---

## 🔄 تحديث الإعدادات

1. عدّل ملف `config.json`
2. ارفعه على GitHub (commit + push)
3. Netlify سينشر تلقائياً خلال ثوانٍ
4. التطبيق سيجلب الإعدادات الجديدة عند الفتح

---

## ⚡ الملفات

| الملف | الوصف |
|-------|-------|
| `config.json` | ملف الإعدادات الرئيسي |
| `index.html` | صفحة إدارة (اختياري) |
| `README.md` | هذا الملف |

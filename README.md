# 🕌 أوقاتي - سيرفر التحكم عن بعد

لوحة تحكم لإدارة إعدادات تطبيق أوقاتي لجميع المستخدمين.

## 🎯 الميزات

- تغيير **اسم التطبيق** لجميع المستخدمين
- تغيير **إصدار التطبيق**
- تغيير **شعار التطبيق** (Logo)
- تفعيل/تعطيل **وضع الصيانة**
- إجبار المستخدمين على **التحديث**
- معاينة مباشرة للتغييرات

## 🚀 كيفية الاستخدام

1. افتح **index.html** في المتصفح
2. عدّل الإعدادات المطلوبة
3. اضغط **"حفظ التغييرات"**
4. انسخ الكود المُنشأ
5. الصق في ملف **config.json**
6. ارفع على GitHub:
```bash
git add config.json
git commit -m "Update app config"
git push origin main
```

## 📁 الملفات

- `index.html` - واجهة لوحة التحكم
- `styles.css` - التصميم
- `app.js` - الوظائف
- `config.json` - ملف الإعدادات

## 🌐 النشر

سيتم نشر الملفات تلقائياً على Netlify عند رفعها على GitHub.

رابط لوحة التحكم: `https://your-netlify-url.netlify.app/`

## 📱 ربط التطبيق

التطبيق يقرأ الإعدادات من:
```
https://your-netlify-url.netlify.app/config.json
```

جميع التغييرات ستظهر فوراً لجميع المستخدمين!
